# Batch LLM Pipeline

- Add steps by populating a node with two functions: `transform()` and `validate()`
- Batch/interactive is abstracted away. Develop fast in interactive, deploy in batch
- Fully type-safe - no hardcoded dicts
- Scales to >1M sets

👉 [demo.ipynb](demo.ipynb) for a worked example

![Style stage data flow](assets/diagram.png)
